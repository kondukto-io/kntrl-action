{
  "name": "kntrl-action",
  "version": "1.0.0",
  "private": true,
  "description": "eBPF-based runtime security agent for GitHub Actions",
  "scripts": {
    "build": "npm run build:start && npm run build:stop",
    "build:start": "ncc build src/start.ts -o dist --source-map --license licenses.txt && cp defaults/supply_chain.rego dist/supply_chain.rego",
    "build:stop": "ncc build src/stop.ts -o stop/dist --source-map --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/io": "^1.1.3",
    "@actions/tool-cache": "^2.0.1",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
